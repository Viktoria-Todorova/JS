function solve() {
    let textToConvert = document.getElementById('text').value;
    let typeOfText = document.getElementById('naming-convention').value;
    let finalText = '';

    let textToLowercase = textToConvert.toLowerCase();
    
    if(typeOfText === 'Camel Case'){

    }else if(typeOfText === 'Pascal Case'){

    }else{
      finalText += 'Eroor!'
    }
    
}